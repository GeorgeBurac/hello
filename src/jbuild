(jbuild_version 1)

(executable
 ((name main)
  (public_name hello)
  (preprocess (pps (ppx_deriving.std)))
  (libraries
   (cmdliner))))

(alias
 ((name    runtest)
 (action  (run ./main.exe everybody))))
